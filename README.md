# Hierarchical-Clustering
Hierarchical clustering builds a tree of clusters. Instead of requiring you to specify the number of clusters upfront, it creates a complete hierarchy from individual data points all the way up to a single cluster containing everything.
